apiVersion: v1
kind: Service
metadata:
  namespace: stage
  name: stage-ti-backed-svc
  labels:
    app: java-springboot
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    app: java-springboot
